# Changelog

Every published version, and what changed in it. A component's own changelog
section on its docs page says the same thing from that component's side.

## Unreleased

- The library scaffold: tokens, Storybook with accessibility checks failing
  rather than warning, unit tests, the contract and token gates, the Astro
  Starlight docs site, and a build that produces a publishable package.
- No components yet.
